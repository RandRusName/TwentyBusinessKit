# Administration

Settings design, compatibility checks, installation/migration state and health
diagnostics for the CRM Application.

## Status

`supporting` — compatibility settings defaults exist. No settings metadata
object is introduced in Phase 6.0.

## Public API

Import only from `src/modules/administration`.
