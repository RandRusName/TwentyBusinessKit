# Catalog

Reusable catalog items, selection contracts and query capabilities.

## Status

`active` — catalog search/categories ports and Twenty adapter are available.
Saved proposal lines remain independent snapshots; catalog changes never rewrite
existing proposals.

## Public API

Import only from `src/modules/catalog`.
